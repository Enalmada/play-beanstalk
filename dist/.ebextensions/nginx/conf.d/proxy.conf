# The default max size is very small causing non-descriptive errors when people upload larger images, etc.
# This is the way to update nginx proxy beanstalk uses to allow bigger client uploads.
client_max_body_size 5M;